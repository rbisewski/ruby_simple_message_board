<!DOCTYPE html>
<html>
<head>
  <title>RubyMsgboard</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

    <div class="navbar navbar-default">
        <div class="container">
            <div class="navbar-brand">
            <%= link_to "Simple Message Board", root_path, class: "navbar-brand navbar-link" %>
            </div>
            <ul class="nav navbar-nav">
                <% if user_signed_in? %>
                    <li><%= link_to "Sign Out", destroy_user_session_path, class: "navbar-text", method: :delete %></li>
                <% else %>
                    <li><%= link_to "Sign Up", new_user_registration_path, class: "navbar-text" %></li>
                    <li><%= link_to "Login", new_user_session_path, class: "navbar-text" %></li>
                <% end %>
            </ul>
            <div><%= link_to "New Message", new_message_path, class: "navbar-brand navbar-right navbar-text" %></div>
        </div>
    </div>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <div class="container">
        <%= yield %>
    </div>

</body>
</html>
